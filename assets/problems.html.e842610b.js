import{ac as n,A as d,B as l,z as e,M as a,H as c,ae as i,O as o}from"./framework.a879f165.js";const t={},r=i(`<div class="custom-container info"><p class="custom-container-title">相关信息</p><p>这里收集的是我遇到过的问题</p></div><h2 id="数据库" tabindex="-1"><a class="header-anchor" href="#数据库" aria-hidden="true">#</a> 数据库</h2><ul><li>Navicat 查看表中的中文是乱码的</li></ul><p>原因：没有什么太多原因，就是编码出了问题<br> 解决：从源头开始排查编码问题，应该编辑一下连接属性，设置编码为自动</p><h2 id="服务器-容器" tabindex="-1"><a class="header-anchor" href="#服务器-容器" aria-hidden="true">#</a> 服务器/容器</h2><ul><li>Tomcat 输出信息时出现中文乱码</li></ul><p>原因：没什么原因，就是编码出了问题，常常出现在 Windows 解决：打开 tomcat 安装目录下的 conf/logging-properties 文件，修改如下参数项</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>java.util.logging.ConsoleHandler.encoding = gbk
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>Tomcat 启动时 CMD 窗口异常退出</li></ul><p>原因：Tomcat 启动时需要找到正确配置的<code>JAVA_HOME</code><br> 解决：环境变量中配置<code>JAVA_HOME</code></p><ul><li>Nginx 不支持中文域名</li></ul><p>原因：中文域名不被标准的解析服务器支持<br> 解决：需要转换为 punycode 码</p><h2 id="node-js" tabindex="-1"><a class="header-anchor" href="#node-js" aria-hidden="true">#</a> Node.js</h2><ul><li>NPM 发布包时提示远程仓库中有相似名的包</li></ul><p>原因：远程仓库中有相似包名的发布包<br> 解决：修改 package.json 中的包名重新发布</p><ul><li>在 powershell 中执行 NPM 脚本被禁止</li></ul><p>原因：powershell 默认策略是防止运行所有脚本文件<br> 解决：切换执行策略</p><p>管理员权限运行 powershell，并键入：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>set-ExecutionPolicy RemoteSigned
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>此时计算机会提示如下信息：选择 Y 或者 A，回车即可</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>执行策略更改
执行策略可帮助你防止执行不信任的脚本。更改执行策略可能会产生安全风险，如 https:/go.microsoft.com/fwlink/?LinkID=135170
中的 about_Execution_Policies 帮助主题所述。是否要更改执行策略?
[Y] 是(Y)  [A] 全是(A)  [N] 否(N)  [L] 全否(L)  [S] 暂停(S)  [?] 帮助 (默认值为“N”):
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="版本控制" tabindex="-1"><a class="header-anchor" href="#版本控制" aria-hidden="true">#</a> 版本控制</h2><ul><li>.gitignore 忽略规则对某些文件失效了</li></ul><p>原因：.gitignore 只能忽略那些原来没有被追踪 (track) 的文件，如果某些文件已经被纳入了版本管理中，则修改 .gitignore 是无效的<br> 解决：本地 git 是有缓存的，清除缓存即可将文件变为未追踪状态</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> <span class="token function">rm</span> <span class="token parameter variable">-r</span> <span class="token parameter variable">--cached</span> <span class="token builtin class-name">.</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>Github 推送时提示：ssh: connect to host github.com port 22: Connection timed out</li></ul><p>原因：可能是防火墙阻塞端口<br> 解决：需要一些操作来确认一下是否为这种原因</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ssh</span> <span class="token parameter variable">-VT</span> git@github.com
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果以上命令得到了 timeout，这种情况应使用 http 协议进行推送，而不是 ssh，只需要将配置文件中的 url 更改为 http 即可</p><p>打开配置文件：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> config <span class="token parameter variable">--local</span> <span class="token parameter variable">-e</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>将配置中的：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>url = git@github.com:username/repo.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>改为：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>url = https://github.com/username/repo.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果看到了 Github 的地址被解析成了 127.0.0.1，则说明被 DNS 污染了，需要获得真正的 IP 并修改 Hosts</p><ul><li>push 到 Github 时产生：fatal: unable to access &#39;https://github.com/xxx/xxx&#39;: OpenSSL SSL_connect: SSL_ERROR_SYSCALL in connection to github.com:443</li></ul><p>原因：这是因为某些东西挡住了 Git 与 Github 之间的连接<br> 解决1：给 Git 配置代理<br> 解决2：尝试 HTTPS 协议推送，而不是 SSH<br> 解决3：尝试改变网络环境，比如使用移动热点尝试，或切换网络运营商</p><h2 id="计算机硬件-操作系统" tabindex="-1"><a class="header-anchor" href="#计算机硬件-操作系统" aria-hidden="true">#</a> 计算机硬件/操作系统</h2><ul><li>主机前面板没有声音</li></ul><p>原因：板载声卡没有驱动<br> 解决：去主板官网下载驱动</p><ul><li>windows 10 无法登陆商店或账户</li></ul><p>原因：可能当前运营商网络访问的 DNS 有问题（对此原因暂时存疑）<br> 解决：设置 DNS 为 4.2.2.2，并<code>cmd ipconfig /flushdns</code>刷新一下，登陆后最好还原 DNS，否则会影响其他的网络访问</p><ul><li>windows 10 20H1 后续版本使用<code>Alt + Tab</code>切换窗口时会将 Edge 中的标签看做成独立的窗口进行切换</li></ul><p>原因：没有任何原因，很 SB 的设计<br> 解决：进入系统设置中的多任务处理选项，将<code>Alt + Tab</code>项切换成<code>仅打开的窗口</code></p><ul><li>使用 SSH 连接 Linux 时出现：Permission denied, please try again</li></ul><p>原因：Linux 远程不允许使用 SSH 连接 ROOT 用户<br> 解决：修改配置文件允许即可</p><p>首先通过别的方式进入 Linux，然后<code>su</code>提权，使用<code>vi /etc/ssh/sshd_config</code>打开配置文件，查看是否有<code>PermitRootLogin no</code>项，如果有就改成<code>PermitRootLogin yes</code>，按<code>ESC</code>退出编辑模式，输入<code>:w!</code>保存，输入<code>:q!</code>退出到命令行，并输入<code>service sshd restart</code>，然后就可以尝试使用 SSH 进行连接了</p><ul><li>某些程序在终端中输出的中文是乱码</li></ul><p>原因：输出程序和终端编码不一致<br> 解决1：改变输出程序的编码方式<br> 解决2：改变终端编码方式</p><p>比如国内 Windows 的终端编码都是 GBK，只要在终端中输入<code>chcp</code>，就会显示：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>活动代码页: <span class="token number">936</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这个<code>936</code>就代表着中文编码，输出其它编码形式的中文时自然会出现乱码，比如要输出的中文是<code>utf-8</code>，在终端中使用<code>chcp 65001</code>命令就会改变成<code>utf-8</code>：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>Active code page: <span class="token number">65001</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>注意这只是临时的，只对本次终端程序有效，启动新的终端还是<code>936</code></p>`,55),u={href:"https://docs.microsoft.com/zh-cn/windows-server/administration/windows-commands/chcp",target:"_blank",rel:"noopener noreferrer"},p=i('<h2 id="编程语言" tabindex="-1"><a class="header-anchor" href="#编程语言" aria-hidden="true">#</a> 编程语言</h2><ul><li>执行 javac 命令可以编译通过，但执行 java 命令时找不到或无法加载主类</li></ul><p>原因：java 命令执行带有<code>./</code>或<code>.\\</code>之类的路径符号，会被错误的认作成一个 package<br> 解决：执行字节码文件时，去掉相关路径符号，也不要跨路径执行，这对于任何终端都是一样</p><ul><li>Python 读取某文件时，最前面有特殊转义字符</li></ul><p>原因：这是因为文件本身是用带签名的<code>utf-8 with BOM</code>保存的<br> 解决1：open 文件时使用<code>utf-8-sig</code>编码<br> 解决2：转换文件格式</p>',5);function h(b,m){const s=o("ExternalLinkIcon");return d(),l("div",null,[r,e("blockquote",null,[e("p",null,[a("更多受支持的"),e("a",u,[a("代码页"),c(s)])])]),p])}const g=n(t,[["render",h],["__file","problems.html.vue"]]);export{g as default};
